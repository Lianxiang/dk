#include <stdlib.h>
#include "room.h"


int room_look(struct room_t *room, char* buf, size_t limit)
{
    return 0;
}

int room_detect(struct room_t *room, char* buf, size_t limit)
{
    return 0;
}


